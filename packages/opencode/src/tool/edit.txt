Replace exact text in an existing file. Read the file first and preserve its indentation without the line-number prefix shown by read.

`oldString` must match exactly once. Include more context when it is not unique, or set `replaceAll` to replace every occurrence.
