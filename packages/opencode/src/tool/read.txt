Read a file or list a directory from an absolute path. Files are returned with 1-based line numbers; directories return one entry per line.

Use `offset` and `limit` for large files, requesting a useful range rather than many tiny slices. Lines over 2000 characters are truncated. Images and PDFs are returned as attachments.
