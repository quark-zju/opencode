Query configured Language Server Protocol servers for definitions, references, hover information, symbols, implementations, and call hierarchies.

Positions are 1-based. `workspaceSymbol` uses `query`; its `filePath` selects the matching server but is not sent in the request.
