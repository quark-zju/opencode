Fetch a fully qualified URL as markdown, text, or HTML. HTTP URLs are upgraded to HTTPS, and large results may be summarized.

Prefer a more targeted available tool when it can retrieve the same content directly.
