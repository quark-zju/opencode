Search file contents with a regular expression. Use `include` to restrict matching files by glob pattern. Returns matching paths and line numbers.

For match counts or other advanced ripgrep behavior, run `rg` with the shell tool.
