Ask the user one or more questions when a decision or missing requirement blocks progress.

A free-form answer is always available; omit `Other` and catch-all options. Answers are arrays of labels or custom text; use `multiple` for multi-select. Put recommendations first with a `(Recommended)` suffix.
